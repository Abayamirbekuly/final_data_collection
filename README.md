# Space Weather Data Pipeline (NOAA API)

Проект по сбору, обработке и аналитике данных о **космической погоде** на основе реального API NOAA (X-Ray Flux). Пайплайн реализован в виде end-to-end data engineering решения с оркестрацией, стримингом, батч-обработкой и аналитикой.

---

## Состав команды (Team Members)

* **Abay Amirbekuly**
* **Daniya**
* **Zhanel**

---

## Технологический стек

| Категория        | Технология                          |
| ---------------- | ----------------------------------- |
| Orchestration    | Apache Airflow                      |
| Streaming        | Apache Kafka                        |
| Processing       | Python (Pandas)                     |
| Storage          | SQLite                              |
| API              | NOAA Space Weather API (X-Ray Flux) |
| Containerization | Docker & Docker Compose             |

---

## Архитектура проекта

Система построена в виде **трёх DAG-ов в Apache Airflow**, каждый из которых отвечает за отдельный этап обработки данных:

### 1⃣ DAG 1 — Pseudo-Streaming (Ingestion)

* Периодичность: каждые **30–60 секунд**
* Источник: NOAA Space Weather API
* Формат: сырой JSON
* Назначение: отправка данных в Kafka
* Kafka Topic: `raw_data_topic`

### 2⃣ DAG 2 — Hourly Batch Processing

* Периодичность: **1 раз в час**
* Источник: Kafka (`raw_data_topic`)
* Обработка:

  * очистка данных
  * нормализация
  * приведение типов
* Инструмент обработки: **Pandas**
* Хранилище: SQLite
* Таблица: `events`

### 3⃣ DAG 3 — Daily Analytics

* Периодичность: **1 раз в сутки**
* Источник: SQLite (`events`)
* Метрики:

  * средняя интенсивность солнечных вспышек
  * максимальная интенсивность за день
* Результат сохраняется в таблицу: `daily_summary`

---

## 📁 Структура репозитория

```text
.
├── airflow/
│   └── dags/              # DAG-файлы Apache Airflow
├── src/                   # Python-скрипты
│   ├── producer.py        # NOAA API → Kafka
│   ├── cleaner.py         # Kafka → Pandas → SQLite
│   └── analytics.py       # Daily analytics
├── data/
│   └── app.db             # SQLite база данных
├── report/
│   └── report.pdf         # Финальный PDF-отчет
├── docker-compose.yml
└── README.md
```

---

## Как запустить проект локально

### 1⃣ Клонировать репозиторий

```bash
git clone <repository-url>
cd space-weather-data-pipeline
```

### 2⃣ Запустить Docker

```bash
docker-compose up -d
```

### 3⃣ Открыть Airflow UI

* URL: `http://localhost:8080`
* Логин: `airflow`
* Пароль: `airflow`

### 4⃣ Запустить DAG-и

Включите следующие DAG-и в Airflow:

* `noaa_streaming_dag`
* `noaa_hourly_batch_dag`
* `noaa_daily_analytics_dag`

---

## Используемые данные

* **Источник**: NOAA Space Weather Prediction Center
* **Тип данных**: X-Ray Solar Flux
* **Характеристики**:

  * timestamp
  * интенсивность солнечных вспышек
  * уровень активности

Все данные являются **реальными** и поступают из официального открытого API NOAA.

---

## Отчет

Полный технический и аналитический отчет доступен по пути:

```text
report/report.pdf
```

Отчет включает:

* описание архитектуры
* обоснование выбора технологий
* примеры данных
* результаты аналитики

---

## Соответствие требованиям задания

✔ Используется **реальное API** из разрешенного списка
✔ Реализованы **3 независимых Job-а (DAG-а)**
✔ Обработка данных выполнена **строго через Pandas**
✔ Присутствует стриминг (Kafka) и батч-аналитика
✔ Проект полностью контейнеризирован

---

## Итог

Данный проект демонстрирует полный цикл работы с потоковыми и аналитическими данными:
от ingestion до хранения и агрегации, и соответствует требованиям **Data Engineering / AI Evaluation Framework**.

Репозиторий готов к защите и сдаче.

